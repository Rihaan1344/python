For the calculator to run properly, in the python file:
  replace the app.run(...) with app.run(debug = True)
and in the template.html (in templates):
  in the fetch address in the js with 127.0.0.1:5000
